`background.js` inside `chrome-extension\src\background\index.ts`
`pages\content-ui` will added components to existent webPage
remove `pages\new-tab`, which will occupy the only one selection for default tab